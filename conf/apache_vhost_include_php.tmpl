#nDeploy#
    <IfModule mod_proxy_fcgi.c>
        <FilesMatch \.php$>
            SetHandler "proxy:unix:/var/run/fpmsockets/[% vhost.user %].sock|fcgi://localhost" 
        </FilesMatch>
    </IfModule>
#nDeploy#
