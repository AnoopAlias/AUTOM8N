#!/bin/bash
if [ $# -eq 1 ]
  then
    if [ $1 == master_down ]
      then
        mysql -uroot -p{{ MYSQLPASS }} -e "STOP SLAVE;"
    elif [ $1 == new_slave ]
      then
        mysql -uroot -p{{ MYSQLPASS }} -e "START SLAVE;"
    fi
fi
