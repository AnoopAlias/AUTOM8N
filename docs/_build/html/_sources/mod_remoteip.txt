mod_remoteip
============

Once you install nDeploy and is using the default "PROXY to apache or apache_SSL" template, apache httpd is sitting behind nginX and has no way to know the actual IP address of the connections
