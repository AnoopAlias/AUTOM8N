testcookie off;
testcookie_name bot_test;
testcookie_session $remote_addr$http_user_agent;
testcookie_arg bot_test;
testcookie_max_attempts 2;
testcookie_pass $good_bots;
testcookie_deny_keepalive on;
testcookie_get_only on;
